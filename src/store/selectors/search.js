export const selectSearchLoading = (state) => state.search.loading

export const selectSearchQuery = (state) => state.search.query

export const selectSearchResults = (state) => state.search.result.entries

export const selectSearchResultsTotal = (state) => state.search.result.total
